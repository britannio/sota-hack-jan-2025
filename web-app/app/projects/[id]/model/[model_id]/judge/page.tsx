

// To escape layout, we need to create a layout.tsx file in the same directory
// that returns null or {children}
export default function JudgePage() {
    return (
        <div>
            <h1>Judge Page</h1>
        </div>
    );
} 